#include <stdio.h>

int main(){
    int days = 70;
    int y = 7;
    printf("There are %d remaining days in the semester.\n", days);
    days=days-y;
    printf("There are %d remaining days in the semester.\n", days);
    return 0;
}

